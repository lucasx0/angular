import { RoxoDirective } from './roxo.directive';

describe('RoxoDirective', () => {
  it('should create an instance', () => {
    const directive = new RoxoDirective();
    expect(directive).toBeTruthy();
  });
});
